<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sign Up - Gestion des Articles</title>
    <script src="https://cdn.tailwindcss.com"></script> <!-- Inclusion de Tailwind CSS via CDN -->
</head>
<body class="bg-gradient-to-b from-gray-800 to-black text-white font-sans">

    <!-- Header -->
    <header class="flex justify-between items-center p-6 bg-gray-900">
        <!-- Logo -->
        <div class="text-white text-xl font-semibold">
            <a href="home.twig" class="text-yellow-400">Article</a> <!-- Remplacez MonLogo par l'image ou le texte de votre logo -->
        </div>
        
        <!-- Menu de connexion -->
        <div class="flex space-x-6">
            <a href="/login" class="text-yellow-400 hover:underline">Login</a>
        </div>
    </header>

    <!-- Formulaire d'inscription -->
    <div class="flex items-center justify-center h-screen"> 
        <form class="bg-gray-800 p-8 rounded-lg shadow-lg w-96" action="/sign-up" method="POST">
            <h1 class="text-3xl font-bold text-center text-yellow-400 mb-6">Sign Up</h1>

            <!-- Champ nom -->
            <div class="mb-4">
                <label for="name" class="block text-gray-300">Nom</label>
                <input type="text" id="name" name="name" class="w-full px-4 py-2 rounded-lg bg-gray-700 text-white focus:outline-none focus:ring-2 focus:ring-yellow-400" required>
            </div>

            <!-- Champ email -->
            <div class="mb-4">
                <label for="email" class="block text-gray-300">Email</label>
                <input type="email" id="email" name="email" class="w-full px-4 py-2 rounded-lg bg-gray-700 text-white focus:outline-none focus:ring-2 focus:ring-yellow-400" required>
            </div>

            <!-- Champ mot de passe -->
            <div class="mb-4">
                <label for="password" class="block text-gray-300">Mot de passe</label>
                <input type="password" id="password" name="password" class="w-full px-4 py-2 rounded-lg bg-gray-700 text-white focus:outline-none focus:ring-2 focus:ring-yellow-400" required>
            </div>

            <!-- Confirmation mot de passe -->
            <div class="mb-4">
                <label for="password_confirmation" class="block text-gray-300">Confirmation Mot de passe</label>
                <input type="password" id="password_confirmation" name="password_confirmation" class="w-full px-4 py-2 rounded-lg bg-gray-700 text-white focus:outline-none focus:ring-2 focus:ring-yellow-400" required>
            </div>

            <!-- Bouton de soumission -->
            <button type="submit" class="w-full bg-yellow-500 text-gray-900 px-6 py-2 rounded-lg hover:bg-yellow-400 focus:outline-none focus:ring-2 focus:ring-yellow-400">S'inscrire</button>
        </form>
    </div>

</body>
</html>