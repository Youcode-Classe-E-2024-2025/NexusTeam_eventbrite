{# Views/Events/event.twig #}
{% extends "base.twig" %}

{% block title %}Events{% endblock %}

{% block content %}
    <h2>Events</h2>
    <ul>
        {% for event in events %}
            <li>{{ event.title }}</li>
        {% endfor %}
    </ul>
{% endblock %}